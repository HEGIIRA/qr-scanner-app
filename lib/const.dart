import 'package:flutter/material.dart';

const primaryColor = Color(0xFFC8A2C8);
const secondaryColor = Color(0xFFFFC1CC);
const tertiaryColor = Color(0xFFFFEBA6);
const accentColor = Color(0xFF8A8A8A);

// animation duratiom time definition
const animationDuration = Duration(milliseconds: 200);

// default constant value for padding size
const defaultPadding = 20.0;